{
  "name": "@ainewshub/api",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/server.js",
  "types": "dist/server.d.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "check": "pnpm --filter @ainewshub/schema build && tsc -p tsconfig.json --noEmit",
    "test": "vitest run src/app.test.ts"
  },
  "dependencies": {
    "@ainewshub/schema": "workspace:*",
    "@hono/node-server": "^1.19.2",
    "hono": "^4.9.7"
  }
}
