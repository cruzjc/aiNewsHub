{
  "name": "@ainewshub/workers",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "pnpm --filter @ainewshub/schema build && tsc -p tsconfig.json --noEmit",
    "test": "vitest run src/pipeline.test.ts"
  },
  "dependencies": {
    "@ainewshub/schema": "workspace:*",
    "fast-xml-parser": "^5.2.5",
    "openai": "^5.20.2"
  }
}
